# Multimodal RAG V2 Dependencies
# Python 3.11 - AWS Lambda (Docker containers)

# Document parsing
PyMuPDF==1.25.5
python-pptx>=0.6.21
python-docx>=1.1.0
beautifulsoup4>=4.12.0
pylatexenc>=2.10

# Numerical — numpy pinned to 1.26.4 for Amazon Linux 2 wheel compatibility
# (numpy >= 2.3 ships only manylinux_2_28 wheels that fail to build on the AL2
# Lambda base image; matches text_generation/math_compute). Not imported
# directly; kept as a version guard.
numpy==1.26.4

# AWS
boto3>=1.34.0
aws-lambda-powertools>=2.40.0
aws-xray-sdk

# Database
psycopg2-binary>=2.9.9
