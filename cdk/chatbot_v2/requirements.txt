# chatbot_v2 Dependencies
# Python 3.11 - AWS Lambda (Docker container)
#
# Version floors pin the supply chain for reproducibility while keeping the
# ">=" style used across the repo (see multimodal_rag_v2) so Amazon Linux 2
# wheel resolution isn't broken by an over-strict "==" pin.

boto3>=1.34.0
botocore>=1.34.0
psycopg2-binary>=2.9.9
httpx>=0.27.0
aws-xray-sdk>=2.14.0
aws-lambda-powertools>=2.40.0
