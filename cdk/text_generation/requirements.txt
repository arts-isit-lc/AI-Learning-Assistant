boto3
langchain==1.2.15
langchain-aws==1.4.4
langchain-core==1.3.0
langchain-community==0.4.1
langchain-classic
langchain-postgres==0.0.17
# Pinned (not imported directly): langchain-aws pulls numpy transitively, and
# numpy >= 2.3 only ships manylinux_2_28 wheels, which fail to install on the
# Amazon Linux 2 Lambda base image (glibc 2.26 / GCC 7.3.1) and fall back to a
# source build that needs GCC >= 9.3. 1.26.4 has a manylinux_2_17 wheel and
# matches the pin used by math_compute and multimodal_rag_v2.
numpy==1.26.4
psycopg2-binary
httpx
aws-xray-sdk
aws-lambda-powertools
