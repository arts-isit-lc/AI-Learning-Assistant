{
  "version": "v1",
  "hooks": [
    {
      "name": "CDK Jest Tests on Test File Edit",
      "description": "Runs the full Jest CDK assertion test suite when a test file is saved. Lib file saves only trigger typecheck (faster, no Docker).",
      "trigger": "PostFileSave",
      "matcher": "cdk/test/.*\\.ts$",
      "action": {
        "type": "command",
        "command": "bash -c 'cd /Users/user/projects/AI-Learning-Assistant/cdk && npm test'"
      },
      "timeout": 120,
      "enabled": true
    }
  ]
}
