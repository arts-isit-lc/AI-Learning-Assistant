---
inclusion: manual
---

# Engineering Log

Durable operational memory for this repo: decisions, deploy scars, workarounds, and
environment facts that are expensive to rediscover and are not already captured in code.

## How to use
- Load before: deploys, incident triage, architecture decisions, Bedrock/model changes, DB/schema changes.
- Inclusion test before adding an entry: *"Would future-me waste time or repeat a mistake without this?"* If it already lives in code or steering, link to it — do not copy it here.
- Knowledge ownership: **steering** = rules to follow · **engineering-log** = decisions & scars · **specs** = current work.
- Conventions: ISO dates; newest-first within each section; decisions get stable IDs `ADR-NNN`; status tags `[Active]` / `[Resolved YYYY-MM-DD]` / `[Superseded by ADR-NNN]`.
- Security: never store secrets, credentials, connection strings, or raw account numbers. Reference secrets by their Secrets Manager path. Keep entries 2-6 lines; link to code/PRs/commits instead of pasting.
- Maintenance: supersede rather than delete; move resolved/superseded items to `## Archive`; keep the file skimmable (~400 lines).

## Architecture Decisions (ADR-lite)
Format: `ADR-NNN · date · status · context -> decision -> consequences`
- ADR-001 · 2026-07-01 · Accepted · `figma-to-code.md` hardcoded design tokens that had drifted from the real theme (listed primary `#2563eb`; actual `#5536DA`) -> made code (`index.css` / `tailwind.config.js` / `Theme.jsx`) the single source of truth for token values; steering now points to it. Consequence: no token values duplicated in steering.

- ADR-002 · 2026-07-01 · Accepted · Recurring multimodal/chatbot bugs are all the same shape — "two paths that should agree, don't" (display vs persist vs grounding; canonical vs projection; ranker score vs display gate) — and the ~841 tests are unit/mocked, so they pass while the seams break -> future work must add a handler-level integration-test layer (real chatbot handler + retrieval→display→persist→reload, AWS/Bedrock mocked) and fix at the seam, not the symptom. Full audit: `.kiro/specs/multimodal-chatbot-audit/findings.md`. **Update 2026-07-01: implemented** — `chatbot_v2/src/test_handler_integration.py` (real `main.handler()`, only AWS/Bedrock/DB IO stubbed) + `test_persist_turn.py`; all audit findings resolved or deferred-with-rationale (see findings.md "Resolution Status").

- ADR-003 · 2026-07-01 · Accepted · Render `message_blocks` (figures/tables/formulas) live in RDS (`Messages.message_blocks`) + the transient SQS projection payload, but the DynamoDB canonical log is text-only (audit M13) -> declare **RDS the authoritative store for message_blocks**; DynamoDB stays the canonical text/history log used for model replay. Consequences: a dropped RDS projection loses blocks (not text); history reload reads blocks from `Messages.message_blocks`; do NOT add a DynamoDB→RDS block rebuild that could resurrect stale blocks.

- ADR-004 · 2026-07-02 · Accepted (pending production validation) · runtime per-query vision ("escalation") added ~5.5s/figure-turn and duplicated ingestion-time perception -> **move perception to ingestion (rich, verbatim-faithful, structured), retrieve+interpret at runtime, keep live vision as a rare feature-flagged fallback**. Offline v2 + Track B eval: stored perception matches/beats live across question categories, and transcription closes the label gap (the weakness was representational, not architectural). Consequences: ENRICHMENT_VERSION bump + corpus backfill, richer stored schema, simpler/faster runtime (~5s off figure turns), flagged fallback. Gate: **Track A production-validation study before implementation.** Full ADR + evidence + design: `.kiro/specs/chatbot-latency-optimization/` (`ADR-perception-at-ingestion.md`, `findings.md`, `production-design.md`).

- ADR-005 · 2026-07-07 · Accepted [validation model superseded by ADR-006 2026-07-09] · Claude 3 Haiku was the prompt-conflict validation model but over-flagged borderline BEHAVIORAL conflicts (the default course prompt — which engages via questions — kept getting reported as incompatible with the Socratic system rule), and no amount of prompt tuning fixed it -> switched the validation model to **Claude 3 Sonnet** via the `/AILA/${env}/ValidationModelId` SSM param default, and added the Sonnet ARN to `dbLambdaRole`'s `bedrock:InvokeModel` (Haiku kept allowed so the model can be switched back at runtime without redeploy). Consequences: better precision on subtle mode/precedence conflicts; higher per-validation latency/cost (occasional instructor action — acceptable); the model id is part of the validation cache key, so the switch auto-invalidates cached reports. **Claude 3.5 is NOT available in ca-central-1 — never propose it.** Code: `api-gateway-stack.ts` (InvokeModel policy + SSM param), `validatePrompt.js` (fallback default), assertions in `iam-policies.test.ts`.

- ADR-006 · 2026-07-09 · Accepted · ca-central-1 has no in-Region access to the Claude 4.5 family, and the old "Claude 3.5 not available here" limit blocked model upgrades, while the chatbot needed a stronger default and the Claude 3 Haiku utility calls were dated -> adopt **Geo-US cross-Region inference profiles** (`us.` prefix) for ALL Anthropic models. Default chatbot (chatbot_v2 `RESPONSE_MODEL_ID`) + text_generation catalog default + new-course default → **Claude Sonnet 4.5** (`us.anthropic.claude-sonnet-4-5-20250929-v1:0`); every former Claude 3 Haiku call (chatbot_v2 eval, enrichment vision + doc-summary, retrieval query-analysis/reasoning/image-escalation, topic extraction) **and the prompt-conflict checker** → **Claude Haiku 4.5** (`us.anthropic.claude-haiku-4-5-20251001-v1:0`). Geo-US from ca-central-1 routes only to ca-central-1 + us-east-1 + us-east-2 + us-west-2. **Data residency: account is set to zero data retention (`data_retention` mode `none`)** so no prompt/response is persisted in ANY Region — processing crosses into the US, storage does not — satisfying the "no data saved outside Canada" requirement. IAM: every Bedrock `InvokeModel*` policy now grants the inference-profile ARN **plus** the underlying `foundation-model` ARN in all four destination Regions, built by the shared helper `cdk/lib/constants/bedrock.ts` (`crisInvokeResources`). Consequences: **supersedes the "Claude 3.5 not available / model swap not an option" constraints** (Bedrock Configuration + the latency note below are updated); `ENRICHMENT_VERSION` bumped `haiku-v5-2026-06`→`haiku45-v6-2026-07` (enrichment cache invalidated → reprocessing on next ingestion); `Courses.llm_model_id` default changed + existing Claude-3-Sonnet rows remapped to Sonnet 4.5 via idempotent `initializer.py` migration (Llama 3 70B rows unchanged; Llama replacement deferred — the `BedrockLLMId` SSM fallback is still Llama); Haiku 4.5 ($1/$5 per M) is ~4× Claude 3 Haiku but ~3× cheaper than Sonnet 4.5, so utility calls stay on Haiku. **`data_retention` mode `none` MUST be set per account (dev + prod) — it is an account/API setting, not CloudFormation (see Operational Runbooks).** Prompt checker is on Haiku 4.5 on a trial basis (ADR-005 moved it to Sonnet-tier for precision; Haiku 4.5 ≫ Haiku 3, so being re-evaluated); Sonnet 4.5 stays IAM-allowed on `dbLambdaRole` so it is switchable at runtime via the `ValidationModelId` SSM param. Code: `constants/bedrock.ts`, `api-gateway-stack.ts` + `multimodal-rag-stack.ts` (IAM), chatbot_v2 + multimodal_rag_v2 model constants, `llm_models.py` + `llmModels.js`, `initializer.py`, `validatePrompt.js`, `generateTopics.js`, `pricing.py`; assertions in `iam-policies.test.ts`, `test_pricing.py`, reasoning/chatbot/doc-summary tests.

- ADR-007 · 2026-07-12 · Accepted · Hypothesis: `Student_Modules.module_score` stuck at 0 was caused by canonical-matching starvation — `evaluation.py`'s `c in module_concepts` exact-string filter dropping the evaluator's demonstrated concepts against long `generated_topics` phrases. The `module_completion_probe` (spec `module-completion-evidence`, Phase 1 — a NON-behavioral log surfacing raw-vs-canonical-filtered evaluator concepts + gate inputs + `missing_requirements`) shipped to dev; the first captured session (**n=1**, "Week 2 Algorithms", 5 topics) showed `eval_kept_demonstrated == eval_raw_demonstrated` on **every** turn (NO loss — the evaluator returned the exact canonical phrases) with `concept_coverage` the sole remaining gate (2/5 demonstrated < 3 required); interactions + engagement cleared on their own -> **DO NOT implement the canonicalization / index-based evaluator fix as a stuck-at-0 remediation** — the hypothesis is unsupported by observed data, and `module_score=0` there is consistent with the completion policy (a completion-expectation question, not a correctness bug). Consequences: behavioral Phase 2 held; the §4.2 index-based evaluator contract is kept as **optional future robustness** (reduce future evaluator drift, not fix a bug) — revisit only if `eval_kept < eval_raw` shows up at scale; keep the probe as ongoing completion-health telemetry; **do NOT change `CONCEPTS_DISCUSSED_COMPLETION_RATIO = 0.5`** until completion-rate + concepts-demonstrated-at-session-end distributions are collected across many sessions; the two-dimensional model is validated — binary `module_score`/`module_complete` (completion) vs. `course_progress.concept_mastery` (per-topic progress), keep separate. Caveat: n=1, directional not conclusive. Code: `chatbot_v2/src/{main.py,evaluation.py,state_machine.py}` (probe + `completion_missing_requirements`); full write-up in the spec's *Phase 1 Outcome*.

- ADR-008 · 2026-07-20 · Accepted · OCELIA rebuild reached a screen-by-screen Figma visual-parity pass (fileKey `9Kt3RPFlZalJnPtCuHLObU`), and several mockup elements had no backing data/endpoint -> decisions: (1) **added backend `GET /instructor/course_messages_rows`** (paginated, ownership-scoped) to back the in-app instructor Chat History table instead of keeping the degraded CSV export — code `cdk/lambda/lib/instructorFunction.js` + `OpenAPI_Swagger_Definition.yaml`, test `cdk/test/instructor-chat-history.test.ts`; (2) locked a **shared modal chrome** (title-over-divider in neutral-900, footer-over-divider, Cancel=`outline`) in `frontend/src/components/ui/dialog.jsx` + `composed/ConfirmDialog.jsx`; (3) instructor names render **"Lastname, Firstname"** everywhere via `utils/formatters.js`. **Intentional divergences from the mockups — NO backing schema/endpoint, forward-compat render only, do NOT file as bugs:** course term/section, Insights "Clear data" (disabled), create-course Term + primary/secondary-instructor distinction (rendered as a flat instructor multi-select), regenerate-access-code (dropped from the Students UI per mockup; `useRegenerateAccessCode` hook left orphaned), and delete-instructor/remove-course = **demote/unenroll** (only `lower_instructor` + unenrollment exist — not a hard account/course delete; confirm copy is honest about that). Consequence: closing these gaps needs new schema/endpoints (a follow-up), not frontend work.

- ADR-009 · 2026-07-20 · Accepted · **Phase 8 legacy purge executed** (user-requested, ahead of finishing per-screen polish). Deleted the entire legacy MUI interface: `LegacyApp` + the `VITE_UI_V2` strangler flag, `App.jsx`/`App.css`, all `src/pages/{admin,instructor,student}` pages + `Login`/`Container`, the 11 legacy top-level components (`AdminHeader`/`InstructorHeader`/`StudentHeader`/`Session`/`FileManagement`/`FileProgressRow`/`FileViewerPanel`/`PdfViewerPanel`/`AIMessage`/`StudentMessage`/`FigureImage`), and `Theme.jsx`. **`main.jsx` now renders `AppV2` directly and owns `Amplify.configure`** (moved out of the deleted `App.jsx`; there is no UI flag anymore). Uninstalled `@mui/material` + `@mui/icons-material` + `@mui/x-date-pickers` + `@emotion/{react,styled}` + `lucide-react` + `material-react-table` + `@aws-amplify/ui-react`; removed the Roboto `@import` + legacy `roboto`/`inter` Tailwind font aliases. **Kept:** `pages/dev` (StyleGuide/Gallery), `components/ErrorBoundary`, the three contexts, and `features/student/chat/*` (the top-level `components/AIMessage|StudentMessage|FigureImage` were legacy dupes). Scars: (1) the Vite **build is the reliable safety net** for over-deletion — it errors on unresolved imports (tests/lint don't); (2) the ~140 long-standing lint errors were **all** in legacy files — lint is now **0 errors** (18 intentional `react-refresh/only-export-components` warnings on colocated test helpers); (3) `NotificationContext.jsx` had a masked unused-`React`-import error surfaced by the purge (fixed). Verified: build ✓, 275 tests ✓. Rest of Phase 8 (OCELIA rebrand, perf targets, WCAG-AA audit) still outstanding.

- ADR-010 · 2026-07-20 · Accepted · **Phase 8 rebrand + performance + a11y gate** (continuation of the ADR-009 purge). **Rebrand:** `index.html` title → "OCELIA", favicon → UBC crest (`public/ubc-logo.svg`), `package.json` name → `ocelia-frontend`, README H1/intro → OCELIA; deleted orphaned legacy `public/*.png` + `src/assets/loginframe.png` (`homepage` already the Amplify URL). **Performance:** added Vite `build.rollupOptions.output.manualChunks` splitting the heavy vendors — `react-pdf`/`pdfjs`, `react-markdown`+`remark`/`rehype`, `react-syntax-highlighter`, `katex`, `recharts`, `aws-amplify` — into their own lazy, cacheable chunks. Result: **StudentChat 1,191→20 KB, index 572→364 KB, InsightsTab 371→5 KB**; largest chunk 239 KB gzipped (under the ≥500 KB-gzipped target). Raised `chunkSizeWarningLimit` to 800 (documented: `vendor-syntax`/`vendor-charts` are large raw but small gzipped + lazy-loaded). **A11y:** added `eslint-plugin-jsx-a11y` (recommended) to the flat ESLint config; resolved every finding — renamed `AppHeader` `role` prop → `userRole` (it collided with the DOM `role` attribute the linter checks), documented the `no-autofocus` exception (autoFocus is intentional dialog/auth-step focus = a11y-correct), added `controlComponents` so the label rule recognises our Radix controls, and disabled `heading-has-content` on the `Card`/`Alert` title primitives (content is supplied by consumers). Gate green: build ✓, lint **0 errors** (18 intentional `react-refresh` warnings), 275 tests ✓. **Environment limits (still open, need a browser / manual review):** Lighthouse Perf≥90 / A11y≥95, full WCAG-AA assistive-tech audit, and the formal ≥30%-vs-baseline bundle confirmation — cannot be run here.

## Deploy History & Gotchas
- [Resolved 2026-07-13] Reset prod to a clean slate via **DROP + recreate** (not TRUNCATE): drop the initializer's tables, then redeploy so the initializer rebuilds them from source — this also retires the schema drift + FK duplication below (fresh tables get every column + FKs once). Key gotcha: **app state spans THREE stores**, so an RDS-only wipe leaves dangling refs — RDS + DynamoDB (`DynamoDB-Conversation-Table` chat history + `AILA-MultimodalRagStack-sessionStateTable`; the embedding/enrichment caches are content-hash keyed = safe to keep) + S3 (IR bucket + data-ingestion bucket). `docs/guides/full-data-wipe.md` was DEV-only (hardcoded dev profile/account + stale bucket names incl. the pre-rename IR bucket, and missed the session-state table) — rewrote it env-parameterized with an `aws sts get-caller-identity` account guard. Always `pg_dump` back up first (`data_export.md`).
- [Active] `initializer.py` foreign-key blocks use **unnamed** `ADD FOREIGN KEY` inside `DO $$ … EXCEPTION WHEN duplicate_object`, but an unnamed FK never raises `duplicate_object`, so every deploy appends another identical copy (prod dump showed each FK ~7x). Harmless but unbounded growth. Deferred fix: give the constraints explicit names so the guard works, + a one-time dedupe. A table wipe-and-recreate also clears the dupes.
- [Resolved 2026-07-13] `initializer.py` schema drift on long-lived DBs: `CREATE TABLE IF NOT EXISTS` **never alters an existing table**, so a persistent DB (prod) is missing every column added to a CREATE TABLE *after* it was first provisioned. Surfaced as `column "status" does not exist` when the `idx_course_modules_status_created` partial index referenced un-added `Course_Modules` lifecycle columns; a schema dump then found 12 missing columns across `Courses`/`Course_Modules`/`Module_Files` (+ `Module_Files.metadata` still `text` vs `jsonb`). Dev hid it (wipe-and-restart ⇒ fresh tables). Fix: idempotent `ALTER TABLE … ADD COLUMN IF NOT EXISTS` backfills for every post-creation column, placed **before** any index/constraint that references them, in `initializer.py` (guards in `test_initializer_schema.py`). **Rule: every new CREATE TABLE column needs a matching ADD COLUMN IF NOT EXISTS migration.** [Active pattern]
- [Resolved 2026-07-13] First deploy of a stack to a SECOND account (initial prod) collides on explicit, non-namespaced S3 bucket names — bucket names are GLOBALLY unique and dev+prod share `StackPrefix=AILA`. `MultimodalRagStack.irBucket` was hardcoded `aila-multimodalragstack-ir-bucket`, already owned by dev (…0264), so CloudFormation refused to create it in prod (…4162): "Resource of type 'AWS::S3::Bucket' ... already exists". Fix: env-namespace the name — `dev` keeps the historical un-suffixed name (its bucket holds RETAINed IR data; renaming orphans it), every other env gets `-${environment}` (prod → `…-ir-bucket-prod`). Code: `multimodal-rag-stack.ts` (irBucket) + assertion `test/ir-bucket-naming.test.ts`. The stack's 3 other buckets omit `bucketName` and let CFN auto-name — never hit this; prefer that pattern for new buckets. Deferred: consistently suffixing dev would replace+orphan its IR bucket (needs re-ingestion) — not done. [Active — dev/prod IR bucket names intentionally asymmetric]
- [Active] Auto-named S3 buckets (no explicit `bucketName` — `dataIngestionBucket`, `chatlogsBucket`, `embeddingStorageBucket`) accumulate ORPHANS on a long-lived account: RETAIN keeps the old bucket every time one is replaced, so `aws s3 ls` shows several same-prefix duplicates (prod had ~2 of each across 04-22/04-29/07-13 deploys). Don't identify the live one by date — ask CloudFormation: `aws cloudformation list-stack-resources --stack-name AILA-ApiGatewayStack --region ca-central-1 --query "StackResourceSummaries[?ResourceType=='AWS::S3::Bucket'].[LogicalResourceId,PhysicalResourceId]" --output table`. Anything in `aws s3 ls` but not in that table is an orphan — safe to empty + `aws s3 rb`. (`describe-stack-resources` under-returns on large stacks — use `list-stack-resources`.) Full steps: `docs/guides/full-data-wipe.md` Step 3.
- [Resolved 2026-07-13] Prod `cdk deploy` failed early validation: "Bootstrap toolkit stack version 30 or later is needed; current version: 28" (plus a red-herring `cloudformation:DescribeEvents` AccessDenied — the v28 deploy role lacks it, so the CLI couldn't fetch error detail; newer bootstrap grants it). Prod's CDK bootstrap stack had drifted behind the pinned CLI (`aws-cdk 2.1118.2`). Fix: re-bootstrap prod with the repo-pinned CLI — `AWS_PROFILE=vincent.adm.prod2 npx cdk bootstrap aws://<prod-acct>/ca-central-1` (needs admin/IAM creds, NOT the deploy role; default qualifier `hnb659fds`); verify SSM `/cdk-bootstrap/hnb659fds/version` ≥ 30. If prod was ever bootstrapped with custom `--cloudformation-execution-policies`/permissions-boundary, re-pass them or they silently reset to `AdministratorAccess`. Unrelated to app code — any prod deploy would have hit it.
- Docker must be running before any deploy/synth: the `predeploy` hook runs `npm test`, which builds 5 container images. No Docker -> immediate failure. Never bypass `predeploy`. [Active]
- `npm run deploy` = test + `cdk deploy --all`; `--all` respects the stack dependency order. Targeted single-stack redeploys must honor that order (graph in `project-overview.md`). [Active]

## Known Bugs & Workarounds
Full audit (2026-07-01): `.kiro/specs/multimodal-chatbot-audit/findings.md` (ranked list + per-finding "Resolution Status").
- [COURSE/MODULE VALIDATION][Active — deferred to interface rebuild] Instructors see two distinct courses as identical when they share department+number ("GEOG 250" `668c9649` vs "GEOG 250/URST 200" `83fc773b`). Root cause is frontend, not backend (all content queries key on `course_id`): `InstructorHomepage.jsx` routes to an unencoded `"{dept} {number} {name}"` string and `CourseDetails` re-derives the course by name (~L164-194), so the `/` in "GEOG 250/URST 200" truncates the `:courseName` route param (~L454) and resolves to the other "GEOG 250" row — the correct `course_id` passed in `navigate` state (~L345) is ignored. Rebuild TODO: (1) validate course + module creation — reject/normalize duplicate `department+number` and names with problematic chars (`/`, stray spacing, "GEOG" vs "Geography" dept variants); (2) route by `course_id` (UUID, slash-free) instead of the derived name — also removes the latent `courseData.find()` first-match collision for any identical name+dept+number; (3) show `course_name` in the admin list, not just `{dept} {number}` (`AdminCourses.jsx:29-38`). No `(department, number)` uniqueness constraint exists today.
- [FRONTEND][Resolved 2026-07-11] Student chat blanked the **entire app** mid-stream, restored on refresh. Root cause was structural, not data: there was **no React error boundary anywhere**, so an uncaught throw while rendering partial, mid-stream content (react-markdown / syntax-highlighter / KaTeX fed the still-growing `streamingText` every chunk) propagated to the React 18 `createRoot` and unmounted the whole tree; refresh reloaded well-formed persisted content and rendered fine. **Red herring — do NOT chase it: `rehype-katex@7.0.1` is internally defensive (two-pass try/catch → `.katex-error` span), so KaTeX does not throw and a `throwOnError:false` option is a no-op.** Fix: reusable `ErrorBoundary` (`frontend/src/components/ErrorBoundary.jsx` — a class, the one React-mandated exception to "functional only") with a `resetKeys` prop; `AIMessage.jsx` wraps each block so a bad partial chunk degrades to raw text and **recovers on the next chunk** when the accumulated text becomes well-formed; `App.jsx` adds a top-level boundary as a catch-all so no render error can blank the page again. Couldn't reproduce the exact throwing chunk (non-deterministic "sometimes"), but the fix is structural and independent of which renderer throws. Verified: ESLint clean on the 3 changed files + `npm run build`. Frontend has no test framework (ESLint-only gate).
- [AUDIT][Resolved 2026-07-01] All 6 HIGH + most MEDIUM/LOW findings fixed at the seam, with tests: chatbot orchestration (H1 loop, persistence, streaming, response-shape parity), retrieval/grounding (figures+tables+formulas, score-scale, figure-ref exactness), enrichment data-loss (TEXT cache, embedding-wipe guard, caption over-match), IDOR ×3 (`get_messages`, `figure_url`, `deleteLastMessage`), display (TableBlock, FigureImage). Handler-level seam tests added (see ADR-002). Verified: 885 py + 3 (deleteLastMessage) + 6 jest + ESLint + py_compile.
- [AUDIT M15][Active] `retrieval_id` is uuid4 (non-deterministic): re-ingesting a file changes all ids, so persisted `message_blocks` figure ids + `figure_url` resolution go stale after re-ingestion. DEFERRED (risky multi-site change); mitigated by the wipe-and-restart workflow. Revisit if incremental re-ingestion becomes routine.
- [AUDIT deferrals][Active] Intentionally NOT changed (rationale in findings.md): L9 IR dedup stays content-only (provenance-aware would keep header/footer noise); L10 no version-scoped GC (needs migration/backfill design); L3/L4/L5 enrichment perf (share conn/secret, parallel embeds, dedup Haiku topic-extraction) — cold-path perf not worth the added risk; L8 query-analyzer keyword lists are curated on purpose (not a bug).
- [FRONTEND][Active] Adding a hook/util to a `src/services/queries/` (or any) `export *` barrel can silently shadow/collide with a same-named export from another module in the barrel, dropping the intended binding. **Mocked component tests don't catch it** (they mock the hook), but the real Vite build does. Rule: run `npm run build` after adding/renaming barrel exports — not just `npm run test`. (Hit during the OCELIA rebuild.)
- `grep_search` is unreliable in this workspace (returned "no matches" for `interactions`, `CREATE TABLE`, `githubRepoName` that demonstrably exist) — verify with read_code / read_file, not grep. [Active]
- psycopg2 import fails in Lambda when the layer was built on Apple Silicon -> psycopg2 is platform-specific and Lambda runs x86_64 -> rebuild from `cdk/`: `./build_layer_x86.sh` (Docker `--platform linux/amd64`, py3.11 -> `layers/psycopg2-py311.zip`). Re-run on Python-runtime or dependency change. [Active]
- `retrieval_units.file_id` must be TEXT, not UUID: the retrieval scope filter binds `file_id = ANY(%s)` as a text[] with no cast, and Postgres has no `uuid = text` operator, so a UUID column makes every scoped vector/BM25 query raise UndefinedFunction and retrieval silently returns nothing. `initializer.py` keeps it TEXT and repairs legacy UUID columns in place. See the `cross-module-file-referencing` spec §4.4. [Active]
- Bumping `IR_VERSION` / `ENRICHMENT_VERSION` / `EMBEDDING_VERSION` in `multimodal_rag_v2` invalidates the corresponding DynamoDB caches -> expect reprocessing cost/latency on next ingestion. [Active]

## AWS Accounts & Environments
- StackPrefix: `AILA`. Environments: `dev` | `prod` — selected via CDK context (`-c StackPrefix=... -c environment=dev|prod`).
- Account + region are env-derived at deploy time (`CDK_DEFAULT_ACCOUNT` / `CDK_DEFAULT_REGION`, see `bin/cdk.ts`), not pinned in code.
- Region: `ca-central-1`. Accounts (last-4; full IDs in `cdk.context.json`): dev = `…0264` (SSO profile `vincent.adm-dev2`), prod = `…4162` (SSO profile `vincent.adm.prod2`).
- Bedrock access is per-account (Anthropic first-time-use approval, once per account) — track which accounts have model access enabled.

## Bedrock Configuration
- Models in use (as of ADR-006, 2026-07-09): **Claude Sonnet 4.5** (default chatbot generation + text_generation catalog default) + **Claude Haiku 4.5** (all utility calls: evaluation, enrichment vision/summary, query analysis, reasoning, image escalation, topic extraction, prompt-conflict checker) + **Llama 3 70B Instruct** (kept as a selectable text_generation option, replacement deferred) + **Titan Embed v2** (embeddings, in-Region). Anthropic models are invoked via **Geo-US cross-Region inference profiles** (`us.` prefix) because ca-central-1 has no in-Region 4.5 access. Version-pinned IDs live in `text_generation/src/constants/llm_models.py` + `frontend/src/constants/llmModels.js` (keep in sync), `chatbot_v2/src/constants/models.py`, the `multimodal_rag_v2` module constants, and the CDK IAM source of truth `cdk/lib/constants/bedrock.ts`. `Courses.llm_model_id` now defaults to the Sonnet 4.5 profile id.
- **Region model access: `ca-central-1` has NO in-Region access to Claude 4.5 (or 3.5). Reach 4.5 via Geo-US cross-Region inference profiles (`us.` prefix), which route to ca-central-1 + us-east-1 + us-east-2 + us-west-2 only. In-Region-only ids (bare `anthropic.claude-...`) will fail here — always use the `us.` profile id as the modelId.**
- **Data retention / residency (ADR-006): account `data_retention` mode is set to `none` (zero data retention) so no prompt/response is persisted in any Region. This is an account/API setting (`PUT` bedrock `data-retention`), NOT CloudFormation — must be set per account (dev + prod) and verified after any account change. Model invocation logging stays OFF (no `CfnModelInvocationLoggingConfiguration`). Do not opt into `provider_data_share` or onboard retention-required models (e.g. Fable 5).**
- Prompt-conflict validation model: **Claude Haiku 4.5** (`us.anthropic.claude-haiku-4-5-20251001-v1:0`), runtime-configurable via the `/AILA/${env}/ValidationModelId` SSM parameter; `dbLambdaRole` may invoke Haiku 4.5 + Sonnet 4.5 (both allowed so it is switchable without a redeploy). On Haiku 4.5 on a trial basis — ADR-005 moved it to Sonnet-tier because Claude 3 Haiku over-flagged; Haiku 4.5 is far stronger, so it is being re-evaluated. Flip the SSM param to the Sonnet 4.5 profile id if precision regresses.
- IAM for cross-Region inference: every `bedrock:InvokeModel*` policy must grant the inference-profile ARN **and** the underlying `foundation-model` ARN in every Geo-US destination Region. Use `crisInvokeResources()` / `inRegionModelResource()` from `cdk/lib/constants/bedrock.ts` — do not hand-write ARNs. Guarded by `iam-policies.test.ts`.
- Adding a model: follow "Adding a Bedrock Model" in `cdk-conventions.md`. Third-party models (Anthropic/Cohere/AI21) need Marketplace subscribe perms; Amazon/Meta models do not.
- Record observed quotas/throttling and any provisioned-vs-on-demand choices here.
- Chatbot response latency (measured via CloudWatch 2026-07-02): breakdown is (1) **image escalation** (vision) inside retrieval's reason step and (2) **generation TTFT**. Escalation now runs its ≤2 vision calls in PARALLEL (`ESCALATION_MAX_IMAGES`, default 2, `image_escalation._analyze_images`) → figure-query escalation ~5–7s → ~2–4s. Generation TTFT is 4.4–9.5s even on the first turn — and the injected RAG context measured only **~250–640 tokens** (`Context build complete.total_token_count`), so TTFT is NOT prefill/context-size bound (an earlier "trim the context" hypothesis was wrong — do not chase it). **TTFT root cause CONFIRMED = the synchronous streaming Bedrock Guardrail** (not prefill, not base model latency). A/B via the `STREAM_GUARDRAIL_DISABLED` diagnostic (2026-07-02, dev): TTFT **guardrail ON avg ~8.1s / median 7.8s (n=12) vs OFF avg ~1.3s / median 1.1s (n=3)** — a ~6.8s / ~6× gap — and ON-TTFT does NOT track `input_tokens` (728→11.5s, 1175→12.0s, 911→4.2s). Mechanism: Bedrock guardrails default to SYNCHRONOUS stream mode (buffer+scan chunks before the first token); ASYNCHRONOUS streams immediately and scans in the background ([docs](https://docs.aws.amazon.com/bedrock/latest/userguide/guardrails-streaming.html)). **Fix path (keep the guardrail): switch to async stream mode** — but `invoke_model_with_response_stream` has no top-level stream-mode param and Claude's Messages API rejects the `amazon-bedrock-guardrailConfig` body key (see `streaming.py` comment), so async requires migrating the streaming call to **`converse_stream`** (`guardrailConfig.streamProcessingMode` enum `sync|async`, confirmed via botocore 1.43.34). Repeatable A/B now wired: `cdk deploy -c streamGuardrailDisabled=true` (DEV-only, prod-gated — `multimodal-rag-stack.ts` + `test/stream-guardrail-toggle.test.ts`). Retrieval-side optimization flags (`RAG_RETURN_PASSAGES` etc.) + `PARALLEL_EVAL_RETRIEVAL` + `CACHE_MODULE_METADATA` are already on. NOTE (updated 2026-07-09, ADR-006): model swap IS now possible via Geo-US cross-Region inference profiles (the earlier "ca-central-1 grants only Claude 3 Sonnet + Haiku" blocker is lifted) — the guardrail-TTFT fix path is still the async `converse_stream` switch, but a faster/smaller generation model is now an available lever if needed. [Active]
- [Enhancement][Active] True "compare figure 4.1 vs figure 4.2" doesn't analyze both figures: `query_analyzer` extracts only the FIRST reference (`_FIGURE_LOOKUP_PATTERN.search`) and `QueryIntent.figure_reference` is single-valued, so escalation looks up one figure. Generic "compare the two diagrams" (no numbers) DOES analyze two (Strategy 2 top-2). Fixing specific-figure comparison needs multi-figure extraction + a list-valued figure_reference + multi-lookup — deferred (out of scope for the latency pass).

## Database Schema Notes
- Engine: RDS Postgres via RDS Proxy (`sslmode=require`). Extensions: `uuid-ossp`, `vector` (pgvector).
- Authoritative schema + migrations: `cdk/lambda/initializer/initializer.py` (SQL embedded in `sqlTableCreation`), run **once at deploy** by the DBFlowStack `TriggerFunction` (`initializer.handler`). Idempotent (`CREATE ... IF NOT EXISTS`, guarded `ALTER` / `DO $$` blocks). No `.sql` files or ORM migrations — this file is the single source of truth.
- Roles created by the initializer: `readwrite` (app CRUD) and `tablecreator`; credentials are generated at init and written to Secrets Manager (user + tablecreator secrets; admin secret used once). Never store those values here.
- Core tables: Users, Courses, Course_Concepts, Course_Modules, Enrolments, Module_Files, Student_Modules, Sessions, Messages, User_Engagement_Log, chatlogs_notifications, Module_File_References.
- Vector table `retrieval_units`: `embedding vector(1024)` (matches EMBEDDING_VERSION `titan-v2-1024`), HNSW cosine index (`vector_cosine_ops`; degrades to exact KNN on pgvector < 0.5.0), BM25 `ts_vector` gin index, `file_id`/`module_id` TEXT scope columns (see the file_id scar above).
- Caches: DynamoDB (embedding + enrichment caches, `multimodal_rag_v2`).

## Operational Runbooks
- Deploy (dev) -> from `cdk/`, after `aws sso login --profile vincent.adm-dev2` (only when the SSO session expired): `AWS_PROFILE=vincent.adm-dev2 npm run deploy`. Runs `npm test` (predeploy) then `cdk deploy --all -c environment=dev --parameters AILA-AmplifyStack:githubRepoName=AI-Learning-Assistant`; `StackPrefix=AILA` comes from `cdk.json`. The profile must have `region=ca-central-1`.
- Deploy (prod) -> from `cdk/`, after `aws sso login --profile vincent.adm.prod2`: `AWS_PROFILE=vincent.adm.prod2 npm run deploy:prod` (gated by `npm test`; `-c environment=prod`, same `StackPrefix=AILA` + repo param). Legacy manual flow (unset + `aws configure export-credentials` + manual `CDK_DEFAULT_*`) is superseded by `AWS_PROFILE` + these npm scripts.
- Tail / query CloudWatch logs (debug) -> ensure a live session first (`aws sso login --profile <profile>`); always pass `--profile` + `--region ca-central-1` (a non-interactive shell won't inherit an exported `AWS_PROFILE`). Profiles: dev `vincent.adm-dev2`, prod `vincent.adm.prod2`. PowerUserAccess covers logs read.
  - List groups: `aws --profile vincent.adm-dev2 --region ca-central-1 logs describe-log-groups --query 'logGroups[].logGroupName' --output text`
  - Live tail: `aws --profile vincent.adm-dev2 --region ca-central-1 logs tail /aws/lambda/AILA-ApiGatewayStack-studentFunction --follow`
  - Recent + filter: `... logs tail <group> --since 2h --filter-pattern '{ $.level = "ERROR" }'` (Powertools logs are JSON; plain `--filter-pattern ERROR` also works). For aggregations use Logs Insights (`logs start-query` / `get-query-results`).
  - Prod: identical commands with `--profile vincent.adm.prod2`.
  - Access is contingent on a live SSO session (can't self-re-auth). Pull scoped windows/filters; never bulk-dump — log contents may hold PII/secrets.
- Rebuild psycopg2 layer -> from `cdk/`: `./build_layer_x86.sh`.
- Enforce zero data retention (required posture for Anthropic 4.5 via CRIS — ADR-006) -> set the account `data_retention` mode to `none` once per account (dev + prod) via the Bedrock `data-retention` control-plane API (account-wide setting: `PUT {"mode":"none"}`; confirm with the matching `GET`). This is NOT CloudFormation-managed — re-verify after account/org changes, and keep Bedrock model invocation logging OFF. Record here once set: dev = [pending], prod = [pending].
- Add a Bedrock model -> follow `cdk-conventions.md` steps (for Anthropic models this means adding the model to `cdk/lib/constants/bedrock.ts` and wiring IAM via `crisInvokeResources`, not hand-written ARNs), then log the account/region enabled here.
- Recover from failed predeploy -> ensure Docker is running, then re-run `npm test` in `cdk/`.
- Apply a DB schema change -> edit `initializer.py` (idempotent SQL) and redeploy DBFlowStack; its `TriggerFunction` re-runs the initializer on deploy.
- Safely redeploy a single stack -> honor the dependency order (project-overview); deploy upstream stacks first if their outputs changed.

## Frontend / OCELIA Rebuild
- [Active] **Phase-1 bundle baseline (2026-07-19, pre-rebuild, current MUI+Tailwind app).** Captured with `cd frontend && npm run build` (Vite 5) BEFORE any Phase 1 change, so the §16 "-30% vs baseline" target has a fixed reference (each build overwrites `dist/`, so this is the only record). Numbers:
  - **Largest JS chunk: `StudentChat` = 1,673.94 kB raw / 504.52 kB gzip** — already **over** the `< 500 kB gzip` target (§16); a primary driver for the rebuild + code-splitting.
  - Next: `InstructorHomepage` 925.30 kB / 264.24 kB gzip · `index` 565.30 kB / 176.36 kB gzip · `TablePagination` (MUI x) 92.99 / 28.89 gzip.
  - **Total JS: 3,277 kB raw / ~1,004 kB gzip** (13 chunks; concatenated gzip 977 kB). **CSS: 378.9 kB raw / 46.3 kB gzip** (MUI + Amplify + KaTeX + Tailwind). `dist/` total 4.8 MB incl. KaTeX web fonts.
  - Targets to beat in Phase 8: total JS gzip ≤ **~703 kB** (-30%) and largest chunk **< 500 kB gzip**. Bundle is MUI/emotion-heavy today; the drop should come mostly from retiring MUI (Phase 8) + per-area code-splitting.
- [Active] **Amplify SPA rewrite ALREADY EXISTS — the Phase 0 audit was wrong on this.** `cdk/lib/amplify-stack.ts` has `amplifyApp.addCustomRule({ ... status: NOT_FOUND_REWRITE })` (the standard "serve index.html for non-asset paths" SPA rule), so `BrowserRouter` deep links / refresh work in prod **with no CDK change**. The audit §7 "No Amplify SPA rewrite → deep links 404" claim came from inspecting only frontend files (`App.jsx`/`package.json`), not the CDK. Don't re-add it. The only routing "hash fix" needed was cosmetic: `frontend/package.json` `homepage` had a trailing `/#/` (Vite ignores `homepage` anyway) — removed.
- [Active] **Phase 2 shell landed behind `VITE_UI_V2`.** `frontend/src/App.jsx` is now a strangler switch: flag `"true"` → new `AppV2` (`src/app/`: `BrowserRouter` + `RequireAuth`/`RequireRole` guards + `AuthContext`/`CourseContext`/`NotificationContext`, route map = audit §7, placeholder screens until Phases 5-7); else → `LegacyApp` (the untouched MUI app). `UserContext` + `Amplify.configure` deliberately stay in `App.jsx` (5 legacy files import `UserContext` from it). `AppV2` is lazy so legacy mode never loads it (separate ~12 kB chunk). Default is legacy — the working app is unaffected until the flag is flipped. Flag + `LegacyApp` deleted in Phase 8. **CourseContext derives course/module IDs from the URL (not state)** — the fix for the legacy "refresh blanks the chat" dead-end.
- [Active] **Phase 3 data layer landed (TanStack Query + Zod over `api.js`), scoped Option A.** Complete infra — `src/services/{http.js,queryClient.js,queryKeys.js,apiError.js,authBridge.js}` + `schemas/student.js` + `queries/*` — but **only the student read-path hooks are built**; instructor/admin hooks are added in their feature phases (P6/P7). Rationale: ~60 endpoints exist but **zero are consumed by the new shell yet**, so wrapping all now = guessing ~60 response shapes with no consumer (the §19 "incremental per-area migration" risk). Contracts are derived from real legacy frontend usage and use `.passthrough()` (validate what we consume; don't drop extra backend fields). **Interceptor lives in the new `http.js` wrapper, NOT the shared `api.js`** — legacy pages keep their exact behavior. Policy: GET retry ×2 backoff, **mutations never** auto-retry; errors → toast unless `error.inline` (403) or `meta.suppressGlobalError`; 401 → silent refresh → retry once → `notifyUnauthorized()` clears auth (AuthProvider registered its `signOut` on the `authBridge`) → RequireAuth redirects. `QueryClientProvider` is inside `AppV2` only.
- [Active] **Testing scar: React Query v5 + Vitest 4 report a rejected-mock query as an *unhandled rejection*** (the failure stack points at the `new Error()` construction site, not an assertion), even with a `QueryCache({ onError: noop })`. Don't test hook-level network errors with a rejected `queryFn` mock — cover error behavior at the `http.js` (normalization) + `queryClient.js` (retry) layers instead, where it lives. Relevant for Phase 5+ component error-state tests (prefer asserting rendered error UI over rejected-promise mocks, or the test will flake).
- [Active] **Phase 4 component library landed.** ~23 primitives in `src/components/ui/` + 17 reusable composed in `src/components/composed/`, all **hand-written shadcn-style** (thin styled wrappers over Radix + `class-variance-authority`, authored directly against the OCELIA tokens) — **not** the `shadcn add` CLI (deterministic, no network/interactive step, OCELIA-token-native from the start). Deps added: `class-variance-authority`, `cmdk`, `@tanstack/react-table`, ~15 `@radix-ui/react-*`. Every component has a colocated RTL test + is rendered in the **`/gallery`** dev route (states shown). Radix gives keyboard/focus/ARIA for free (the a11y DoD). **Feature-coupled composed are deferred to their feature phases** (chat → P5; module tree/wizard/upload/prompt-history/analytics → P6) per audit §10 — building them without their data/interaction context would just be rework. `Toast` stays **react-toastify** (no Radix Toast).
- [Active] **jsdom test polyfills for Radix.** `src/test/setup.js` polyfills `ResizeObserver`, `Element.prototype.scrollIntoView`, `hasPointerCapture`, `releasePointerCapture` — Radix ScrollArea/Select/DropdownMenu touch these and jsdom lacks them. **Radix Select's full open-interaction still can't be driven in jsdom** even with the polyfills (pointer-capture semantics) — test the closed combobox in RTL and defer open/keyboard-select coverage to Playwright (Phase 5+). Same applies to any component that opens Radix Select.
- [Active] **Phase 5 student slice landed (behind `VITE_UI_V2`).** `/courses` (StudentHome + join-by-code), `/courses/:courseId` (CourseView + Learning Journey drawer), `/courses/:courseId/modules/:moduleId` (StudentChat). All in `src/features/student/`, on the Phase 3 hooks + Phase 4 components. RHF + Zod (`@hookform/resolvers`) for the join form. Legacy `pages/student/*` + `useChatSession` untouched (strangler; removed in P8).
- [Active] **Chat streaming ported verbatim into `useChatStream`** (the legacy Option-B contract: AppSync WS `onChatChunk` authoritative, `API_KEY` WS auth, `POST chatbot-v2` fire-and-forget, 130s watchdog, idempotent `finalizeTurn`, `parseBlocks`, auto-rename, `update_module_score`). **Reconciled with Query:** transient `streamingText`/optimistic bubble are local; the final message is written to the `useSessionMessages` cache via `setQueryData`. **Greeting-clobber guard:** a freshly-created session's `useSessionMessages` is disabled (`createdRef`) so its empty server history doesn't overwrite the streamed greeting (the legacy `skipHistoryFetchRef` bug). The per-block `ErrorBoundary` in `AIMessage` is preserved (chat-blanking fix). **Streaming can't be exercised in jsdom/CI** — tested via a MockWebSocket unit test (chunk→accumulate→done→cache; error→retry); **live AppSync streaming needs manual QA in dev.**
- [Active] **First Playwright smoke authored** (`e2e/student-chat.spec.js`: login→course→chat). It **skips unless** `E2E_STUDENT_EMAIL`/`E2E_STUDENT_PASSWORD` are set AND the dev server runs the new shell (`VITE_UI_V2=true`), plus `npx playwright install`. Can't run in this environment.
- [Active] **Phase 6 instructor slice landed (behind `VITE_UI_V2`).** Courses list + tabbed course workspace (`/instructor/courses/:courseId/{configuration,insights,settings,chat-history,students}`) + 4-step create wizard (`modules/new`) + single-page editor (`modules/:moduleId/edit`), all in `src/features/instructor/` on the Phase 3 hooks + Phase 4 components. Data split: cacheable reads/writes → Query hooks (`services/queries/instructor-*.js`, `schemas/instructor.js`); imperative/realtime (upload XHR, processing poller, draft reserve/cleanup, `onNotify` WS) → feature hooks in `features/instructor/hooks/` (see `frontend-conventions`). `@dnd-kit` added for accessible reorder. Legacy `pages/instructor/*` untouched (strangler; removed P8). The 5 P6 composed (`ModuleAccordion`/`WizardStepper`/`FileUpload`/`PromptHistory`/`AnalyticsChart`) complete the registry.
- [Active] **Configuration = one Concept→Module tree** (the audit's model), consolidating the legacy separate "Edit Concepts" / "Edit Modules" tabs. Concept + module reorder via `@dnd-kit` (nested SortableContexts, drag handles). **Assumption: `reorder_module` gets `module_number` assigned sequentially *within a concept* (tree-native), not globally** — legacy numbered a flat list; backend semantics unverified. Revisit if ordering looks wrong across concepts. Concept-delete cascade is **client-orchestrated** (delete each child `delete_module_s3` then `delete_concept`) — backend **B6** (server-side mark-and-sweep) will replace it; until then the frontend cascades.
- [Active] **Settings = conflict-check-on-save** (no separate "check" button, per audit): Save runs `validate_prompt`; on `has_conflicts` it blocks + shows conflicts; a second Save opens an override confirm → saves with `conflict_metadata` (which persists the Settings-tab dot until the prompt is edited conflict-free). Validation failure degrades to save-allowed. `useValidatePrompt` (module scope) is reused by the wizard + editor.
- [Active] **Test scars (Phase 6).** (1) `@dnd-kit` **renders** in jsdom but the drag **gesture** can't be driven — test the reorder handler + persist mutation + pure helpers (`groupConceptTree`, `arrayMove`), defer drag to Playwright (same shape as the Radix-Select scar). (2) **Recharts** isn't unit-testable in jsdom (no layout/ResizeObserver) — stub its primitives or mock `AnalyticsChart`; assert `role="img"` + composition. (3) `vi.mock` factories **can't reference top-level `const`s** (hoisting) — use `vi.hoisted` for `apiClient`/`http`/`subscribe` mocks. (4) `useJobNotification`/WS tested via a `MockWebSocket` + `vi.stubEnv` (drive `onopen`→`start_ack`→`data`).
- [Active] **Build scar: barrel re-export name collisions pass Vitest but FAIL the Rollup build.** Two `useModuleFiles` exports (student `files.js` + instructor `instructor-module.js`) merged fine under Vitest's ESM namespace (last-wins) but broke `npm run build` ("Conflicting namespaces … not exported by index.js"). Renamed the instructor one → `useModuleAllFiles`. **Rule: keep `services/queries` hook names globally unique, and always run `npm run build` (not just `vitest`) at every checkpoint — the build catches export collisions the test runner hides.**
- [Active] **Backend gaps consumed by Phase 6 (frontend degrades; no CDK change this phase).** **B5** (chat-logs rows-as-JSON + server-side `.xlsx`): Chat History ships the **degraded CSV-download** experience (`fetch_chatlogs` presigned URLs + generate via `onNotify`); the in-app virtualized table + Excel export slot in when B5 lands. **B4** (per-instructor / course Active-Inactive access): the audit maps course Active/Inactive to existing `course_student_access`, but **no write route exists in the client** — the instructor course-status toggle is **omitted** (courses list shows all as clickable, no status badge) pending the route. **B6**: concept-delete cascade is client-side (above). These are a parallel backend track, not part of the frontend phase.
- [Active] **Phase 7 admin slice landed (behind `VITE_UI_V2`).** Instructor management + course management as master-detail on `SplitLayout` (`/admin/instructors/:instructorId` with the email as an encoded param; `/admin/courses/:courseId` + `/admin/courses/new`), all in `src/features/admin/` on `services/queries/admin-*.js` + `schemas/admin.js`. Every admin CRUD is covered by the existing 12 admin Lambda routes — **no backend change**. Legacy `pages/admin/*` untouched (removed P8). No new registry components (composed existing ones).
- [Active] **Admin domain facts worth not rediscovering.** (1) **Assignment edits = delete-all-then-re-enroll REPLACE** — there's no single unassign route, so `useSetInstructorCourses`/`useSetCourseInstructors` DELETE all enrolments then re-`enroll_instructor` the selected set. (2) **"Remove instructor" = demote** (`lower_instructor`: drops the instructor role + instructor enrolments), NOT a hard user delete. (3) Admin instructor/role state is **server-side `Users.roles[]`**, not Cognito groups; `elevate_instructor` even creates a pending `Users` row before signup (hence "awaiting signup" when names are null). (4) **Course Active/Inactive uses `admin/updateCourseAccess`** — this route DOES exist, so the toggle is built admin-side (it's the toggle P6 correctly omitted instructor-side, where no route exists). (5) Access codes are **client-generated at create** (`XXXX-XXXX-XXXX-XXXX`); no server mint/regenerate route — the detail shows the code read-only.
- [Active] **Barrel-collision avoided (applying the P6 build scar).** The admin "courses an instructor is assigned to" hook is named **`useInstructorAssignedCourses`**, NOT `useInstructorCourses` — the latter is already exported by `instructor-courses.js`, and a duplicate barrel export passes Vitest but breaks the Rollup build. `npm run build` (off + on) verified green.
- [Active] **Backend gaps flagged in Phase 7 (parallel track, not built here).** **B2** `duplicate_course`: does not exist (verified frontend + `adminFunction.js`) → the **Duplicate action is omitted** (a client-side copy can't clone concepts/modules/files, so it'd be misleading). **B4** per-instructor `Enrolments.access_enabled`: does not exist → the **per-instructor "OCELIA access" toggle is omitted**. Both live only in the plan doc.
- [Active] **Design-fidelity correction pass (2026-07-20).** Build feedback: the initial rebuild diverged from the frames; corrected against the Figma components as source of truth. (1) **Low-radius system** — controls (button/input/select/textarea) are SQUARE (`rounded-none`), containers (card/dialog/menu) get 4px (`rounded-sm`); the old 6/8/12px scale over-rounded everything. (2) `--border` = **#808080** (Arts ISIT Neutral/600), not #BFBFBF; added `--border-subtle` #BFBFBF for hairlines. (3) Cards are **flat** (`--shadow-card: none`); modals/menus use a **hard offset** shadow `4px 4px 2px rgba(0,0,0,.2)`. (4) Added `--primary-subtle` #F2E8FF + a `neutral-*` tailwind scale. (5) `AppHeader` shows the account **email** — `AuthContext` read it from the Cognito **access** token (no `email` claim → showed the username UUID); fixed to merge **ID-token** claims. (6) Create/Edit module are **centered modals** over Configuration (nested routes → `ConfigurationTab` `<Outlet/>`), not full pages; `Tag` → purple-outline pill; `FileUpload` → single click/drag dropzone. Tokens live in `frontend/src/index.css` + `tailwind.config.js`.
- [Active] **Student course-card progress → batched `GET /student/progress_summary` (backend track B7, 2026-07-20).** `student/course` carries no progress, so the home card's percent + "x/y concepts completed" comes from ONE SQL aggregate (concept complete when its active modules average 100 — mirrors course_page / Learning Journey), NOT a per-card `course_page` fetch (avoids N+1; progress is its own cache, invalidated independently of the stable list). No new IAM (reuses `studentFunction`/`dbLambdaRole`); inherits the central email-vs-authorizer 401 guard. Client `useCourseProgressSummary` is disabled in instructor-preview (progress is student-enrolment scoped). Tests: `cdk/test/student-progress-summary.test.ts` (OpenAPI contract + handler unit 200/400/404/401) + FE hook/component tests; `tsc --noEmit` clean. Full CDK Jest suite (Docker) NOT run — change is OpenAPI YAML + Node handler + test only.

## Glossary
- AILA — StackPrefix / project shorthand (AI Learning Assistant).
- Roles — Admin, Instructor, Student.
- `multimodal_rag_v2` — 4-layer RAG pipeline (ingestion -> enrichment -> retrieval -> reasoning).
- `chatbot_v2` — structured-learning chatbot. `math_compute` — SymPy compute Lambda. `text_generation` — LangChain generation Lambda.
- DBFlow — DBFlowStack; deploys after ApiGatewayStack, before AmplifyStack; runs the DB initializer.

## Archive
<!-- move [Resolved] / [Superseded] entries here to keep the sections above skimmable -->
